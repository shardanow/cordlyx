{
  "name": "cordlyx",
  "version": "0.1.0",
  "private": true,
  "description": "Project and issue tracking system",
  "workspaces": [
    "packages/*",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w packages/shared && npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w packages/shared && npm run test -w backend && npm run test -w frontend",
    "test:e2e": "playwright test",
    "lint": "npm run lint -w packages/shared && npm run lint -w backend && npm run lint -w frontend",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w backend && npm run typecheck -w frontend",
    "clean": "npm run clean -w packages/shared && npm run clean -w backend && npm run clean -w frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@swc/cli": "^0.8.1",
    "@swc/core": "^1.15.43",
    "concurrently": "^9.1.0",
    "playwright": "^1.61.1",
    "typescript": "^5.7.0"
  }
}
